1. 
   - На бэке проверялся параметр `placement`, хотя отправлялся и использовался далее `placement_id`
   - Поправил везде нейминг на `placement_id`.
   - Проверялось на фронте, вывод корректный.
   
   
2.
   - В БД просто вставлялась запись через INSERT без условий, проверок или подготовки. 
   - Сделал принудительное удаление перед вставкой выбранной даты. Можно было также сделать через UPSERT.
   - Проверялось повторным прогоном `aggregate:daily 2026-08-07`


3.
   - При агрегации захардкожена зона UTC. 
   - Берём теперь таймзону из `.env` (по факту - из окружения докера), либо UTC как fallback.
   - Проверялось добавлением новых событий с другим временем и повторной агрегацией, либо же опять `aggregate:daily 2026-08-07` 


4. 
   - В main.go handleStat() отвечает за `/stat`, т.е. приём событий. Нет никаких проверок.
   - Добавил сюда обработку query-параметров.
   - Проверялось запросами на `/stat`, при кривых параметрах возвращается 422 с ошибками.

### Тесты
Написаны на PHP, запускаются командой:
```bash
docker compose exec php php tests/Run.php
```

### Путь данных
1. `/stat` - прилетают события и помещаются в таблицу `raw_events`
2. `php aggregate:daily` - агрегирует данные из `raw_events` и сохраняет в `daily_stats`. Условно - формируем отчёт из событий и сохраняем в БД. [`DailyStatsAggregator.php`]
3. На дашборде отображается выбор даты, выбор PlacementID из `/api/placements` [`PlacementRepository.php`] и отчёт по выбранной дате с заданным `placement_id` [`StatsRepository.php`]

`/api/daily-stats` просто выводит информацию из агрегированной таблицы `daily_stats`



